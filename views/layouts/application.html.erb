<html>
  <%
        def link_to(name, url)
            return "<a href='#{url}'>#{name}</a>"
         end
  %>
  
  <form method='post' action="<%= @request.path %>">
            First name: <input type="text" name="first_name" placeholder='first name'/><br />
            <input type="radio" name="sex" value="male">Male<br>
            <input type="radio" name="sex" value="female">Female<br>
            <input type="submit" value="Submit" />
  </form>
  
  <% if @request.query['sex'] == "male" && !@request.query['first_name'].empty? %>
    <p> Welcome Mr.  <%= @request.query['first_name'] %></p>
  <% elsif @request.query['sex'] == "female" %>
    <p> Welcome Ms. <%= @request.query['first_name'] %> </p>  
  <% end %>
  
  <body>
    <h1>You're using a layout</h1>
    <p>
      <h3> I'm a header</h3>
      <p>Put things in the header</p>
      <%= link_to('Links to google', 'http://www.google.com') %>
      <%= link_to('links to reddit', 'http://www.reddit.com') %>
      <a href='me.html'>Me</a>

    </p>
    <hr />
    <div>
      <%= yield %>
    </div>
    <hr />
    <div>
      <h3> I'm a footer</h3>
      <p>Put things in the footer</p>
    </div>
  </body>
</html>
